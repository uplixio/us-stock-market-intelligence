{
  "name": "us-stock-market-intelligence",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "npm --prefix frontend run dev",
    "build": "npm --prefix frontend run build",
    "start": "npm --prefix frontend run start",
    "vercel-install": "npm --prefix frontend ci"
  }
}

